{
  "name": "@unifi/knowledge-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Tech Elixir Knowledge & Institutional Memory Agent",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "prebuild": "npm run build --workspace=@unifi/shared-core",
    "build": "tsc --project tsconfig.json",
    "pretest": "npm run build --workspace=@unifi/shared-core",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@unifi/shared-core": "1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.5.3",
    "vitest": "^4.1.9"
  }
}
